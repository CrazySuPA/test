git config
git config --global user.name zyw
git config --global user.email 2664942926@qq.com231231
git config --list 查看git的配置情况

git log

git init
git status   查看文件是否被追踪：绿色，追踪，  红色，未被追踪

git add   将未被追踪的文件由工作区添加到暂存区，然后文件变为暂存的状态
git add . 将本地所有未被追踪的文件添加到暂存区

git commit -m ""  将暂存区内的文件提交到版本库
git commit -am "" 跳过git add添加暂存区命令，直接将工作区所有已跟踪的文件一次性提交到版本库中

git结构和状态：
git的3层结构：working directory 工作区  staging index 暂存区  git directory（Repository）版本库
git中文件的四种状态：
untracked：未被追踪
modified：表示工作区修改了某个文件但是还没有添加到暂存区
staged：表示把工作区修改的文件添加到了暂存区但是没有提交到版本库
committed：表示数据被安全的存储在本地库中










